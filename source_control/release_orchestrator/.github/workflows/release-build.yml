name: release-build
on:
  workflow_dispatch:
jobs:
  call-reusable:
    uses: asterion/actions-infra/.github/workflows/reusable-build.yml@hotfix/cache-key
    permissions:
      id-token: write
      contents: read
